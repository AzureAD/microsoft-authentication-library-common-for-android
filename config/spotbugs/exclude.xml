<?xml version="1.0" encoding="UTF-8"?>
<FindBugsFilter>
<Match>
    <!-- This warns because the same constructor argument has a NonNull check in both subclass and superclass -->
    <Bug pattern="RCN_REDUNDANT_NULLCHECK_WOULD_HAVE_BEEN_A_NPE" />
</Match>
<Match>
    <!-- This warns when there's a null check on NonNull parameters.
         Given that we haven't enforced this strictly with AndroidX Nonnull,
         (which doesn't really do anything), we'll ignore this for now.
         Once we migrated everything to Lombok's Nonnull (which will throw an
         exception), we can remove this.
    -->
   <Bug pattern="RCN_REDUNDANT_NULLCHECK_OF_NONNULL_VALUE" />
 </Match>
<Match>
    <!-- This warns when there's a transient field in a serializable class -->
    <Bug pattern="SE_TRANSIENT_FIELD_NOT_RESTORED" />
</Match>
<Match>
    <!-- This warns when a field is never read. -->
    <Bug pattern="URF_UNREAD_FIELD" />
</Match>
<Match>
    <!-- Return value of method without side effect is ignored. -->
    <!-- Adding this primarily for CryptoFactoryTelemetryHelper where this shows up -->
    <Class name="com.microsoft.identity.common.java.opentelemetry.CryptoFactoryTelemetryHelper" />
    <Bug pattern="RV_RETURN_VALUE_IGNORED_NO_SIDE_EFFECT" />
</Match>
<Match>
    <!-- Return value of method without side effect is ignored. -->
    <Package name="com.microsoft.identity.common.java.commands.*" />
    <Bug pattern="RV_RETURN_VALUE_IGNORED_NO_SIDE_EFFECT" />
</Match>
    <Match>
        <!-- Spotbugs doesn't seem to work well with kotlin...
        https://github.com/spotbugs/spotbugs/issues/573 -->
        <Source name="~.*\.kt"/>
    </Match>
<Match>
    <!-- SpotBugs 6.x flags `new SecureRandom().nextBytes(...)` here. This detector targets
         java.util.Random (create-and-use-once yields poor randomness); for SecureRandom a fresh,
         properly-seeded instance used once is secure and correct, so this is a false positive.
         Scoped to this class to keep the detector active everywhere else. -->
    <Class name="com.microsoft.identity.common.java.providers.oauth2.PkceChallenge" />
    <Bug pattern="DMI_RANDOM_USED_ONLY_ONCE" />
</Match>
<Match>
    <!-- The Gradle 9 upgrade bumped spotbugs-gradle-plugin 4.7.x -> 6.x, whose detectors report
         these medium-confidence patterns on pre-existing, unchanged code that 4.7.x did not.
         They are suppressed here (repo-local) so behaviour matches the pre-upgrade baseline
         without disabling detection for other repos. Follow-up: revisit and fix, especially the
         concurrency ones (AT_*, USO_*), then remove those entries. -->
    <Bug pattern="AT_NONATOMIC_64BIT_PRIMITIVE,AT_STALE_THREAD_WRITE_OF_PRIMITIVE,CT_CONSTRUCTOR_THROW,DCN_NULLPOINTER_EXCEPTION,EI_EXPOSE_REP,EI_EXPOSE_REP2,EI_EXPOSE_STATIC_REP2,HSM_HIDING_METHOD,MS_EXPOSE_REP,NP_NONNULL_FIELD_NOT_INITIALIZED_IN_CONSTRUCTOR,PA_PUBLIC_PRIMITIVE_ATTRIBUTE,SING_SINGLETON_HAS_NONPRIVATE_CONSTRUCTOR,SS_SHOULD_BE_STATIC,SSD_DO_NOT_USE_INSTANCE_LOCK_ON_SHARED_STATIC_DATA,US_USELESS_SUPPRESSION_ON_CLASS,US_USELESS_SUPPRESSION_ON_FIELD,US_USELESS_SUPPRESSION_ON_METHOD,USO_UNSAFE_ACCESSIBLE_OBJECT_SYNCHRONIZATION,USO_UNSAFE_METHOD_SYNCHRONIZATION,USO_UNSAFE_STATIC_METHOD_SYNCHRONIZATION" />
</Match>
</FindBugsFilter>
