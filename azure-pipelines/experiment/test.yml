
name: "test"

trigger: none
pr: none

resources:
  repositories:
  - repository: self
    type: git
    ref: master

jobs:
- job: Phase_1
  displayName: Phase 1
  cancelTimeoutInMinutes: 1
  pool:
    name: Hosted Windows 2019 with VS2019
  steps:
  - checkout: self
    clean: true
    submodules: recursive
    persistCredentials: True
  - task: PowerShell@2
    inputs:
      targetType: 'inline'
      workingDirectory: $(Build.SourcesDirectory)/azure-pipelines/
      script: |
        # Write your PowerShell commands here.
        dir
        cd auth-client
        dir
        Write-Host "Hello World"