package com.microsoft.identity.client.ui.automation.interaction;

/**
 * The prompt behaviour expected during a given authorization request
 */
public enum PromptParameter {
    SELECT_ACCOUNT,
    LOGIN,
    CONSENT
}
